#include <tunables/global>

profile alarm_detector_v9 flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  # Home Assistant base image / s6-overlay
  /init ix,
  /bin/** ix,
  /usr/bin/** ix,
  /run/{s6,s6-rc*,service}/** ix,
  /package/** ix,
  /command/** ix,
  /etc/services.d/** rwix,
  /etc/cont-init.d/** rwix,
  /etc/cont-finish.d/** rwix,
  /run/{,**} rwk,
  /dev/tty rw,

  # Add-on options and future add-on-owned profiles
  /data/** rw,

  # Application and Python runtime
  /app/** r,
  /app/run.sh rix,
  /usr/bin/python3* ix,
  /usr/lib/python3*/** r,
  /lib/** rm,
  /usr/lib/** rm,

  # Home Assistant audio socket and temporary ALSA configuration
  /run/audio/pulse.sock rw,
  /tmp/** rwk,
  /var/tmp/** rw,

  # Local REST event publishing through the Supervisor proxy
  network inet stream,
  network inet6 stream,

  # Name resolution
  /etc/nsswitch.conf r,
  /etc/hosts r,
  /etc/resolv.conf r,
  /etc/host.conf r,
  /etc/gai.conf r,
}
